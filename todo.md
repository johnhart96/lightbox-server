# Todo

## Bugs
* Service status on dashboard does not work.
* System uptime does not work on the dashboard
* Docker IP is populated with the docker IP and not the host IP.
* Service logs do not work.

## Features
* DHCP leases should automaticlly be added as DNS entries
* Discover devices by ACN and automatically add DNS entries (ANSI E1.17)
* Assign one fileshare as a TFTP Share & Implement TFTP
* Add a settings dialog that will at first allow you to enable or disable services (docker start / docker stop)
* Ability to export and import the config via the web ui (download and upload lightbox.db)
* User authentication for the web UI and for file sharing
* Ability to set the entertainment network IP in the settings UI
* Add a certificate authority.
* Reverse DNS
* Devices tab. See all devices (both DHCP added and mamually added) and see if they are online.